import { IonContent, IonHeader, IonPage, IonTitle, IonToolbar } from '@ionic/react';
import './Carte.css';

const Carte: React.FC = () => {
  return (
    <IonPage>
      <IonContent fullscreen>
      <h5 className="title">Carte</h5>
      </IonContent>
    </IonPage>
  );
};

export default Carte;
